version: 2.0

jobs:
  build:
    docker:
      - image: circleci/node:12.18.2
    working_directory: ~/repo
    steps:
      - checkout
      # Bundle install dependencies
      - run: npm install 
      - run: npm run test:unit